services:
  db:
    image: postgres:16
    container_name: role_model_db
    environment:
      POSTGRES_USER: rolemodel
      POSTGRES_PASSWORD: rolemodel
      POSTGRES_DB: role_model
    ports:
      - "5433:5432"
    volumes:
      - role_model_pgdata:/var/lib/postgresql/data

volumes:
  role_model_pgdata:
